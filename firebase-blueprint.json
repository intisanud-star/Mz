{
  "entities": {
    "users": {
      "title": "User",
      "description": "A user in the Exona platform.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "places": {
      "title": "Place",
      "description": "A school, business, or community space created by a user.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "category": { "type": "string", "enum": ["School", "Business", "Community", "Personal", "Other"] },
        "description": { "type": "string" },
        "logo": { "type": "string" },
        "creatorUid": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "isOfficial": { "type": "boolean" }
      },
      "required": ["name", "category", "creatorUid", "timestamp"]
    },
    "records": {
      "title": "Record",
      "description": "A general record (student, customer, or personal) with privacy settings.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "subjectName": { "type": "string", "description": "Name of the person or entity the record is about." },
        "category": { "type": "string" },
        "amountPaid": { "type": "number" },
        "balance": { "type": "number" },
        "type": { "type": "string", "enum": ["general", "books", "uniforms", "services", "products"] },
        "visibility": { "type": "string", "enum": ["public", "private", "shared"] },
        "sharedWith": { "type": "array", "items": { "type": "string" } },
        "placeId": { "type": "string" },
        "creatorUid": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["subjectName", "category", "visibility", "creatorUid", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": { "schema": "users", "description": "User profiles" },
    "/places/{placeId}": { "schema": "places", "description": "User-created places/institutions" },
    "/records/{recordId}": { "schema": "records", "description": "General records linked to places" }
  }
}
